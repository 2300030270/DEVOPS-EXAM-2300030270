const config = {
    url: "http://localhost:2030/2300030270_backend" // ` is used to combine both String  
}

export default config;

// const config = {
//   url: "http://localhost:2000"
// };

// export default config;
